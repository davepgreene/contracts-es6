const { getMethods, compareMethods } = require('./utils/methods');
const { getArguments, compareArguments } = require('./utils/arguments');

module.exports = {
  getMethods,
  getArguments,
  compareArguments,
  compareMethods,
};
